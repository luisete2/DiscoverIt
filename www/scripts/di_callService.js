 function runPost(url, params) {

};
function run(url, params) {
    // $rootScope.start = new Date().getTime();
    // console.info("init" + url);
    /*var deferred = $q.defer();
    return $http.get(url, {
        params: params
    }).then(function (response) {
        deferred.resolve(response.data);
    //             $rootScope.end = new Date().getTime();
    // console.info("end" + url, $rootScope.end -  $rootScope.start);                    
        return deferred.promise;
    }, function (response) {
        deferred.reject(response);
        return deferred.promise;
    });*/
};
